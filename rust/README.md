To run the rust solution, ensure you have cargo installed and a measurements.txt file generated.

### Build with optimizations

```
$> cargo build --release
```

```
$> ./target/releases/hundred-million-row-challenge measurements.txt
```
